# Disabled - not needed for this fork. Protected via .gitattributes merge=ours.
name: 'Close stale issues'
on: workflow_dispatch
jobs:
  stale:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled"
